export const itemSearcherByCode = (history, keywords) => {
  console.log('search item by code ' + keywords);
};


export const serviceSearcherByCode = (history, keywords) => {
  console.log('search service by code ' + keywords);
};